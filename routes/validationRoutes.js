const express = require('express');
const router = express.Router();

// Reserved for future route-level validation middleware.
// Kept as a placeholder to avoid breaking current route mounting.

module.exports = router;

